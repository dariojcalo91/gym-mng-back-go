# Database
DB_USER=gopher
DB_PASSWORD=gympassword
DB_NAME=gym_management
DB_PORT=5432
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}?sslmode=disable

# Security
AES_MASTER_KEY=change-me-to-a-32-byte-secret-key
JWT_SECRET=change-me-to-another-32-byte-secret
